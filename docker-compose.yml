services:
  api-server:
    build: ./backend
    container_name: avi-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/usr/src/app

  web-app:
    build: ./frontend
    container_name: avi-frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/usr/src/app